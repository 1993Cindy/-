int search(int array[],int n,int v)
{
	int left,right,mddle;
	left = 0,right = n-1;
	while(left<=right){
		middle = (left+right)/2;
		if(array(middle)>v)
		{
			right = midlle;
		}else if(array[middle]<v)
		{
			left = middle;
		}else{
		return middles;
		}
	}
	return -1;
}
